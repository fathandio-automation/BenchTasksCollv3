# Final Pool of Implemented Tasks

This folder contains all implemented tasks from the BenchTasksCollv3 project.
A task is considered "implemented" if it satisfies the requirements specified in `tasks/examples/example-task/`.

## Implemented Tasks (from most recent commits on each dev branch)

| Task | Implementor |
|------|-------------|
| discount-calculator | fan-dev |
| tag-manager | gyy |
| robots-handler | gyy |
| media-organizer | haoze |
| help-desk | junteng_dev |
| social-connector | junxian_dev |
| client-portal | lueyang-dev |
| log-analyzer | ruige |
| cache-optimizer | wenshuo-dev |
| status-checker | xiaochen_dev |
| health-monitor | xiaochen_dev |

## Implementation Requirements

Based on the example task (`tasks/examples/example-task/`):
- `docs/task.md` - Required, non-empty, all English
- `docs/agent_system_prompt.md` - Required, non-empty, all English
- `docs/user_system_prompt.md` - Optional, but if non-empty, all English
- `evaluation/` - Required folder with `main.py`
- `preprocess/` - Required folder with `main.py`
- `initial_workspace/` - Optional
- `groundtruth_workspace/` - Optional